// * 5. Funcion con tipado: Escribe una función add que acepte dos números como argumentos y devuelva su suma. Asegúrate de tipar adecuadamente los parámetros y el valor de retorno.

function add(num1: number, num2: number): number {
    return num1 + num2
}
const suma = add(4, 6)

console.log(suma);